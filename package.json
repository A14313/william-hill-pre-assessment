{
	"name": "william-hill-pre-assessment",
	"version": "1.1.0",
	"description": "This is my solution to the pre assessment of William Hill",
	"scripts": {
		"start": "rm -rf dist/* && parcel src/*.html --open",
		"build": "rm -rf dist/* && parcel build src/*.html --public-url ./"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@parcel/transformer-sass": "^2.9.3",
		"parcel": "^2.9.3",
		"sass": "^1.63.6"
	}
}
